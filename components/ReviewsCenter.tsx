"use client";

import { FormEvent, useEffect, useState } from "react";
import { Check, Copy, MessageCircle, Send, Share2, Star } from "lucide-react";
import { ReviewThread } from "./ReviewThread";

type Review = { id: string; authorName: string; country: string; service: string; rating: number; review: string; createdAt: string };
const shareUrl = "https://swiftverify-alpha.vercel.app/reviews";

export function ReviewsCenter({ initialReviews = [] }: { initialReviews?: Review[] }) {
  const [reviews, setReviews] = useState(initialReviews);
  const [count, setCount] = useState(initialReviews.length);
  const [status, setStatus] = useState("");
  const [copied, setCopied] = useState(false);
  useEffect(() => { const refresh = () => fetch("/api/reviews", { cache: "no-store" }).then((response) => response.ok ? response.json() : null).then((data) => { if (data) { setReviews(data.reviews); setCount(data.count); } }).catch(() => undefined); refresh(); const interval = window.setInterval(refresh, 30000); return () => window.clearInterval(interval); }, []);
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setStatus("Publishing your review..."); const form = event.currentTarget; const data = new FormData(form); const response = await fetch("/api/reviews", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ firstName: data.get("firstName"), lastName: data.get("lastName"), country: data.get("country"), service: data.get("service"), rating: data.get("rating"), recommendation: data.get("recommendation"), email: data.get("email"), review: data.get("review") }) }); const result = await response.json(); setStatus(result.message || result.error); if (response.ok) form.reset(); }
  function share(network: "facebook" | "whatsapp" | "instagram") { const text = "Read client reviews on SwiftVerify Reviews Center"; const urls = { facebook: `https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(shareUrl)}`, whatsapp: `https://wa.me/?text=${encodeURIComponent(`${text}: ${shareUrl}`)}`, instagram: `https://www.instagram.com/?url=${encodeURIComponent(shareUrl)}` }; if (network === "instagram") { navigator.clipboard?.writeText(shareUrl); setCopied(true); setTimeout(() => setCopied(false), 2500); } window.open(urls[network], "_blank", "noopener,noreferrer"); }
  return <div className="space-y-12"><section className="grid gap-8 lg:grid-cols-[1fr_0.85fr] lg:items-start"><div><div className="flex items-end gap-4"><p className="font-display text-6xl font-semibold tracking-[-0.06em] text-[#081624]">{count}</p><p className="pb-2 text-sm font-bold uppercase tracking-[0.16em] text-[#14845e]">published reviews</p></div><p className="mt-3 max-w-md text-base leading-7 text-slate-600">Read practical experiences from freelancers and online sellers who wanted clearer next steps through remote work, payment platforms and verification requirements.</p><div className="mt-7 flex flex-wrap gap-3"><button type="button" onClick={() => share("facebook")} className="inline-flex items-center gap-2 rounded-full border border-slate-200 px-4 py-2.5 text-sm font-bold text-[#1877f2] transition hover:bg-slate-50"><Share2 className="h-4 w-4" /> Facebook</button><button type="button" onClick={() => share("whatsapp")} className="inline-flex items-center gap-2 rounded-full border border-slate-200 px-4 py-2.5 text-sm font-bold text-[#14845e] transition hover:bg-slate-50"><MessageCircle className="h-4 w-4" /> WhatsApp</button><button type="button" onClick={() => share("instagram")} className="inline-flex items-center gap-2 rounded-full border border-slate-200 px-4 py-2.5 text-sm font-bold text-[#c13584] transition hover:bg-slate-50"><Send className="h-4 w-4" /> Instagram</button></div>{copied && <p className="mt-3 text-xs text-slate-500">Review link copied for Instagram sharing.</p>}</div><div className="rounded-[1.35rem] border border-[#c9ded3] bg-[#eef6f1] p-6"><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#14845e]">Publish your review</p><h2 className="mt-3 font-display text-2xl font-semibold text-[#081624]">Help someone take their next step.</h2><form onSubmit={submit} className="mt-6 space-y-3"><div className="grid gap-3 sm:grid-cols-2"><input required name="firstName" placeholder="First name" autoComplete="given-name" className="min-h-11 w-full rounded-xl border border-white bg-white px-3.5 text-sm outline-none focus:border-[#14845e]" /><input required name="lastName" placeholder="Last name" autoComplete="family-name" className="min-h-11 w-full rounded-xl border border-white bg-white px-3.5 text-sm outline-none focus:border-[#14845e]" /></div><input required type="email" name="email" placeholder="Email for review updates" autoComplete="email" className="min-h-11 w-full rounded-xl border border-white bg-white px-3.5 text-sm outline-none focus:border-[#14845e]" /><div className="grid gap-3 sm:grid-cols-2"><input required name="country" placeholder="Country" className="min-h-11 w-full rounded-xl border border-white bg-white px-3.5 text-sm outline-none focus:border-[#14845e]" /><select required name="rating" defaultValue="" className="min-h-11 w-full rounded-xl border border-white bg-white px-3.5 text-sm outline-none focus:border-[#14845e]"><option value="" disabled>How many stars?</option>{[5, 4, 3, 2, 1].map((value) => <option key={value} value={value}>{value} stars</option>)}</select></div><input required name="service" placeholder="Service for review" className="min-h-11 w-full rounded-xl border border-white bg-white px-3.5 text-sm outline-none focus:border-[#14845e]" /><select required name="recommendation" defaultValue="" className="min-h-11 w-full rounded-xl border border-white bg-white px-3.5 text-sm outline-none focus:border-[#14845e]"><option value="" disabled>Would you recommend us to someone you know?</option><option value="yes">Yes, I would recommend SwiftVerify</option><option value="no">No, I would not recommend SwiftVerify</option></select><textarea required name="review" rows={4} placeholder="Write your review..." className="w-full resize-y rounded-xl border border-white bg-white px-3.5 py-3 text-sm outline-none focus:border-[#14845e]" /><button type="submit" className="inline-flex min-h-11 w-full items-center justify-center gap-2 rounded-full bg-[#14845e] px-5 text-sm font-bold text-white transition hover:bg-[#0f704f]">Publish review <Send className="h-4 w-4" /></button>{status && <p role="status" className="flex items-start gap-2 text-xs leading-5 text-[#315346]"><Check className="mt-0.5 h-3.5 w-3.5 shrink-0" />{status}</p>}</form></div></section><section><div className="mb-6 flex items-center justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#14845e]">Live wall</p><h2 className="mt-2 font-display text-3xl font-semibold text-[#081624]">What clients are saying</h2></div><button type="button" onClick={() => { navigator.clipboard?.writeText(shareUrl); setCopied(true); setTimeout(() => setCopied(false), 2500); }} className="hidden items-center gap-2 text-sm font-bold text-[#14845e] sm:inline-flex"><Copy className="h-4 w-4" /> Copy link</button></div>{reviews.length === 0 ? <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center text-sm text-slate-500">Every client journey is different. Share your experience to help others find a clearer, more confident path forward.</div> : <div className="grid gap-5 md:grid-cols-2">{reviews.map((review) => <article key={review.id} className="rounded-[1.25rem] border border-slate-200 bg-white p-6"><div className="flex gap-1 text-[#14845e]" aria-label={`${review.rating} out of 5 stars`}>{Array.from({ length: review.rating }).map((_, index) => <Star key={index} className="h-4 w-4 fill-current" />)}</div><blockquote className="mt-4 whitespace-pre-line text-base leading-7 text-[#1e3345]">“{review.review}”</blockquote><div className="mt-6 border-t border-slate-100 pt-4"><p className="font-display font-semibold text-[#081624]">{review.authorName}</p><p className="mt-1 text-sm text-slate-500">{review.service} · {review.country}</p></div><ReviewThread reviewId={review.id} /></article>)}</div>}</section></div>;
}
